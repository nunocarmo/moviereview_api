server:
  port: ${PORT:8080}

spring:
  output:
    ansi:
      enabled: ALWAYS
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: ${HIBERNATE_DDL_AUTO:update}
      show-sql: true
  datasource:
    platform: postgres
    url: ${POSTGRES_URL:jdbc:postgresql://lucky.db.elephantsql.com:5432/stsxddtq}
    username: ${POSTGRES_USERNAME:stsxddtq}
    password: ${POSTGRES_PASSWORD:CffTTQQgV1oVoMl04pOGIWGPhOwbzC4H}
    driverClassName: org.postgresql.Driver